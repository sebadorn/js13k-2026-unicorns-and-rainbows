import { LevelObject } from './LevelObject.js';


export class Player extends LevelObject {


	/**
	 * 
	 * @param {Level} level
	 * @param {number} x
	 * @param {number} y
	 */
	constructor( level, x, y ) {
		super( level, x, y );
	}


};
